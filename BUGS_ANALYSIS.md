# Анализ багов и потенциальных проблем

## 🔴 Критические проблемы безопасности

### 1. Отсутствие валидации webhook подписи от ЮКассы
**Файл:** `backend/src/controllers/payments.controller.ts:24-27`
**Проблема:** Валидация подписи webhook закомментирована, что позволяет злоумышленникам отправлять поддельные уведомления о платежах.
```typescript
// Валидация подписи (в продакшене обязательно!)
// if (!validateWebhookSignature(event, signature)) {
//   console.error('Invalid webhook signature')
//   return
// }
```
**Риск:** Активация подписки без реальной оплаты.
**Решение:** Раскомментировать и реализовать проверку подписи.

### 2. Публично доступный endpoint для напоминаний
**Файл:** `api/reminders.ts:241-248`
**Проблема:** Если `CRON_SECRET` не установлен, endpoint доступен всем, что позволяет злоупотреблять ресурсами.
```typescript
if (cronSecret) {
  // проверка
} else {
  console.warn('⚠️ CRON_SECRET not set - endpoint is publicly accessible!')
}
```
**Риск:** DDoS атаки, расход ресурсов.
**Решение:** Обязательно требовать `CRON_SECRET` в продакшене.

### 3. Неполная валидация webhook подписи
**Файл:** `backend/src/utils/yookassa.ts:116-125`
**Проблема:** Функция `validateWebhookSignature` всегда возвращает `true`, даже в продакшене.
```typescript
export function validateWebhookSignature(data: any, signature: string): boolean {
  if (process.env.YUKASSA_TEST_MODE === 'true') {
    return true
  }
  return true // ❌ Всегда true!
}
```
**Риск:** Поддельные webhook уведомления.
**Решение:** Реализовать проверку подписи согласно документации ЮКассы.

### 4. Отсутствие проверки токена бота в serverless функциях
**Файл:** `api/bot.ts:8-13`, `api/reminders.ts:10-12`
**Проблема:** Если токен отсутствует, код продолжает выполняться, что может привести к ошибкам.
```typescript
if (!token) {
  console.error('❌ TELEGRAM_BOT_TOKEN is not set')
  // ❌ Нет return или process.exit()
}
```
**Риск:** Непредсказуемое поведение при отсутствии токена.
**Решение:** Добавить ранний return или throw error.

### 5. Использование non-null assertion для переменных окружения
**Файл:** `backend/src/controllers/payments.controller.ts:5-6`, `backend/src/controllers/subscription.controller.ts:5-6`
**Проблема:** Использование `!` для переменных окружения без проверки может привести к ошибкам в runtime.
```typescript
const SHOP_ID = process.env.YUKASSA_SHOP_ID! // ❌ Может быть undefined
const SECRET_KEY = process.env.YUKASSA_SECRET_KEY!
```
**Риск:** Ошибки при отсутствии переменных окружения.
**Решение:** Проверять наличие переменных и выбрасывать понятную ошибку.

### 6. Webhook endpoint без защиты от CSRF
**Файл:** `backend/src/routes/payments.routes.ts:8`
**Проблема:** Webhook endpoint доступен без дополнительной защиты, полагается только на подпись (которая не проверяется).
**Риск:** Поддельные запросы к webhook.
**Решение:** Добавить проверку IP-адресов ЮКассы или использовать middleware для валидации.

---

## 🟠 Проблемы с обработкой ошибок

### 7. Webhook возвращает 200 до обработки
**Файл:** `api/bot.ts:66-79`, `backend/src/controllers/payments.controller.ts:12-16`
**Проблема:** Webhook отвечает 200 OK до завершения обработки. Если обработка упадет, Telegram/ЮКасса не узнают об ошибке.
```typescript
// ЮКасса требует быстрого ответа (200 OK)
res.status(200).json({ received: true })
// Обработка происходит после ответа
```
**Риск:** Потеря данных, невозможность повторной обработки.
**Решение:** Использовать очередь задач или логировать все ошибки для ручной обработки.

### 8. Ошибки в webhook не логируются должным образом
**Файл:** `backend/src/controllers/payments.controller.ts:99-102`
**Проблема:** Ошибки в webhook просто логируются, но не сохраняются для анализа.
```typescript
} catch (error: any) {
  console.error('❌ Error processing webhook:', error)
  // Не возвращаем ошибку, чтобы ЮКасса не пыталась повторно
}
```
**Риск:** Потеря важных данных о платежах.
**Решение:** Сохранять ошибки в БД для последующего анализа.

### 9. Обработчик ошибок Express не логирует детали
**Файл:** `backend/src/index.ts:42-45`
**Проблема:** Ошибки логируются, но не сохраняются и не отправляются в систему мониторинга.
```typescript
app.use((err: any, req: express.Request, res: express.Response, next: express.NextFunction) => {
  console.error('Error:', err)
  res.status(500).json({ error: 'Internal server error' })
})
```
**Риск:** Сложность отладки в продакшене.
**Решение:** Добавить структурированное логирование и интеграцию с Sentry/другим мониторингом.

---

## 🟡 Race conditions и конкурентный доступ

### 10. Race condition при создании привычки
**Файл:** `backend/src/controllers/habits.controller.ts:143-180`
**Проблема:** Проверка лимита и создание привычки не атомарны. Два одновременных запроса могут обойти лимит.
```typescript
// Проверка лимита
const userWithSubscription = await prisma.user.findUnique({...})
if (!hasPremium && userWithSubscription.habits.length >= FREE_HABITS_LIMIT) {
  return res.status(403).json({...})
}
// ❌ Между проверкой и созданием может быть создана другая привычка
const habit = await prisma.habit.create({...})
```
**Риск:** Превышение лимита привычек для free пользователей.
**Решение:** Использовать транзакцию с блокировкой или уникальный индекс в БД.

### 11. Race condition при отметке привычки
**Файл:** `backend/src/controllers/habits.controller.ts:386-448`
**Проблема:** Хотя есть обработка дубликатов, логика может быть улучшена.
```typescript
const existingLog = await prisma.habitLog.findFirst({...})
if (existingLog) {
  // Удаление
} else {
  // Создание - может упасть с P2002
}
```
**Риск:** Дублирование логов или потеря данных.
**Решение:** Использовать `upsert` вместо `findFirst` + `create`.

### 12. Неатомарное обновление статуса подписки
**Файл:** `backend/src/middleware/subscription.ts:26-32`
**Проблема:** Проверка и обновление статуса происходят в два запроса.
```typescript
if (!isActive && userWithSubscription.subscriptionStatus === 'active') {
  await prisma.user.update({...}) // ❌ Может быть устаревшим
}
```
**Риск:** Несогласованность данных.
**Решение:** Использовать транзакцию или оптимистичную блокировку.

---

## 🟡 Логические ошибки

### 13. Дублирование логики проверки подписки
**Файл:** `backend/src/controllers/habits.controller.ts`, `backend/src/middleware/subscription.ts`
**Проблема:** Логика проверки подписки дублируется в нескольких местах, что усложняет поддержку.
**Риск:** Несогласованность при изменении логики.
**Решение:** Вынести в единую утилиту.

### 14. Неэффективные запросы к БД
**Файл:** `backend/src/middleware/subscription.ts:12-14`
**Проблема:** Делается дополнительный запрос к БД, хотя пользователь уже есть в `req.user`.
```typescript
const userWithSubscription = await prisma.user.findUnique({
  where: { id: user.id } // ❌ user уже есть в req.user
})
```
**Риск:** Лишние запросы к БД, снижение производительности.
**Решение:** Использовать данные из `req.user` или загружать подписку один раз в middleware.

### 15. Неправильная обработка часовых поясов в напоминаниях
**Файл:** `api/reminders.ts:123-149`
**Проблема:** Функция `convertLocalTimeToUTC` может некорректно обрабатывать переход через границы дня.
```typescript
if (utcHours < 0) {
  utcHours += 24
} else if (utcHours >= 24) {
  utcHours -= 24
}
// ❌ Не учитывается изменение даты
```
**Риск:** Напоминания могут отправляться в неправильное время.
**Решение:** Использовать библиотеку для работы с часовыми поясами (например, `date-fns-tz`).

### 16. setTimeout в serverless функции
**Файл:** `backend/src/controllers/subscription.controller.ts:201-230`
**Проблема:** Использование `setTimeout` в serverless функции может не выполниться, если функция завершится раньше.
```typescript
setTimeout(async () => {
  // Проверка статуса платежа
}, 5000)
```
**Риск:** Платежи могут остаться в статусе `pending`.
**Решение:** Использовать очередь задач (например, Vercel Queue) или проверять статус при следующем запросе.

---

## 🟡 Проблемы с валидацией данных

### 17. Недостаточная валидация reminderTime
**Файл:** `backend/src/middleware/validation.ts`
**Проблема:** Нет валидации формата времени для `reminderTime` (должно быть "HH:MM").
**Риск:** Некорректные данные в БД, ошибки при отправке напоминаний.
**Решение:** Добавить валидацию формата времени.

### 18. Отсутствие валидации telegram_id на фронтенде
**Файл:** `frontend/src/utils/telegram.ts:37-76`
**Проблема:** Если `telegram_id` не получен, запрос все равно отправляется.
**Риск:** Лишние запросы к API, которые заведомо упадут.
**Решение:** Проверять наличие `telegram_id` перед отправкой запросов.

### 19. Нет проверки на пустые строки после trim
**Файл:** `backend/src/controllers/habits.controller.ts:185`
**Проблема:** После `trim()` строка может стать пустой, но это не проверяется.
```typescript
name: name.trim(), // ❌ Может быть пустой строкой
```
**Риск:** Создание привычек с пустыми именами.
**Решение:** Проверять после `trim()` или использовать валидацию middleware.

---

## 🟢 Проблемы с тестовым кодом

### 20. Тестовый режим включен в продакшене
**Файл:** `backend/src/utils/streak.ts`, `backend/src/controllers/habits.controller.ts`
**Проблема:** `TEST_MODE = true` в нескольких местах, что меняет логику работы приложения.
**Риск:** Неправильная работа streak и напоминаний в продакшене.
**Решение:** Удалить весь тестовый код или использовать переменную окружения.

### 21. Хардкод значений в коде
**Файл:** `backend/src/controllers/habits.controller.ts:171`, `backend/src/middleware/subscription.ts:75`
**Проблема:** `FREE_HABITS_LIMIT = 3` захардкожен в нескольких местах.
**Риск:** Сложность изменения лимита.
**Решение:** Вынести в константу или конфигурацию.

---

## 🟢 Проблемы с производительностью

### 22. N+1 запросы при получении привычек
**Файл:** `backend/src/controllers/habits.controller.ts:31-61`
**Проблема:** Для каждой привычки вызывается `calculateStreakFromLogs`, что может быть неэффективно.
**Риск:** Медленная загрузка при большом количестве привычек.
**Решение:** Оптимизировать запросы или использовать кэширование.

### 23. Загрузка всех логов для каждой привычки
**Файл:** `backend/src/controllers/habits.controller.ts:13-22`
**Проблема:** Загружаются до 100 логов для каждой привычки, даже если нужны только последние.
```typescript
logs: {
  orderBy: { date: 'desc' },
  take: 100 // ❌ Может быть слишком много
}
```
**Риск:** Большой объем данных, медленные запросы.
**Решение:** Ограничить количество или загружать только необходимые данные.

---

## 🟢 Проблемы с типами и TypeScript

### 24. Использование `any` типов
**Файл:** Множество файлов
**Проблема:** Использование `(req as any).user` вместо правильных типов.
**Риск:** Потеря типобезопасности.
**Решение:** Создать правильные типы для Express Request.

### 25. Отсутствие проверки на null/undefined
**Файл:** `api/reminders.ts:214`
**Проблема:** `telegramId.toString()` может упасть, если `telegramId` null.
```typescript
const chatId = Number(habit.user.telegramId.toString())
```
**Риск:** Ошибки при выполнении.
**Решение:** Добавить проверки на null/undefined.

---

## 🟢 Другие проблемы

### 26. Отсутствие rate limiting
**Проблема:** Нет ограничения частоты запросов к API.
**Риск:** DDoS атаки, злоупотребление ресурсами.
**Решение:** Добавить rate limiting (например, `express-rate-limit`).

### 27. CORS настроен слишком широко
**Файл:** `backend/src/index.ts:15-26`
**Проблема:** В продакшене разрешены любые домены через `true` в массиве origins.
```typescript
...(process.env.NODE_ENV === 'production' ? [true] : [])
```
**Риск:** CSRF атаки.
**Решение:** Указать конкретные разрешенные домены.

### 28. Отсутствие мониторинга и алертов
**Проблема:** Нет системы мониторинга ошибок и метрик.
**Риск:** Проблемы обнаруживаются слишком поздно.
**Решение:** Интегрировать Sentry, DataDog или аналогичный сервис.

### 29. Нет обработки отключения Prisma Client
**Файл:** `api/reminders.ts:264-266`
**Проблема:** `$disconnect()` вызывается только в `finally`, но не при ошибках в других местах.
**Риск:** Утечки соединений.
**Решение:** Обеспечить правильное закрытие соединений везде.

---

## 📋 Приоритеты исправления

### Критично (исправить немедленно):
1. Валидация webhook подписи от ЮКассы (#1, #3)
2. Защита endpoint напоминаний (#2)
3. Проверка переменных окружения (#5)
4. Race condition при создании привычки (#10)

### Высокий приоритет:
4. Webhook обработка ошибок (#5, #6)
5. Race condition при отметке привычки (#9)
6. setTimeout в serverless (#14)

### Средний приоритет:
7. Дублирование логики (#11)
8. Неэффективные запросы (#12, #20, #21)
9. Валидация данных (#15, #16, #17)

### Низкий приоритет:
10. Тестовый код (#18)
11. Типы TypeScript (#22, #23)
12. Мониторинг (#26)
