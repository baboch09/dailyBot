# 🚀 Варианты деплоя на Vercel

## Вариант 1: Через npx (без установки) ⭐ Рекомендуется

```bash
cd /Users/ebabochiev/Desktop/tg
npx vercel
```

**Преимущества:**
- ✅ Не требует глобальной установки
- ✅ Всегда использует последнюю версию
- ✅ Работает сразу

## Вариант 2: Через веб-интерфейс Vercel

1. **Зайдите на [vercel.com](https://vercel.com)**
2. **Add New Project** (или **Import Project**)
3. **Выберите GitHub** (или создайте репозиторий)
4. **Настройки проекта:**
   - **Root Directory:** `./`
   - **Framework Preset:** Other
   - **Build Command:** `cd backend && npm install && npx prisma generate && npm run build && cd ../frontend && npm install && npm run build`
   - **Output Directory:** `frontend/dist`
   - **Install Command:** `cd backend && npm install && cd ../frontend && npm install`

5. **Environment Variables:**
   ```
   DATABASE_PROVIDER = postgresql
   ```

6. **Deploy!**

После деплоя:
- Создайте PostgreSQL: Storage → Create Database → Postgres
- Добавьте `FRONTEND_URL` с вашим доменом Vercel
- Примените миграции (см. VERCEL_DEPLOY.md)

## Вариант 3: Исправить npm и установить глобально

Если у вас есть права sudo:

```bash
# Исправить права на npm кэш
sudo chown -R $(whoami) ~/.npm

# Очистить кэш
npm cache clean --force

# Установить Vercel
npm i -g vercel
```

Затем:
```bash
vercel login
vercel
```

## Какой вариант выбрать?

- **Для быстрого старта:** Вариант 1 (npx vercel) ✅
- **Если есть GitHub репозиторий:** Вариант 2 (веб-интерфейс) ✅
- **Если нужна постоянная установка:** Вариант 3 (исправить npm)

---

## После любого варианта

1. Создайте PostgreSQL в Vercel Dashboard
2. Настройте переменные окружения
3. Примените миграции
4. Обновите Bot URL в BotFather

См. подробности в `VERCEL_DEPLOY.md`
