# 🚀 Деплой в продакшен (без ngrok)

## Рекомендуемые варианты (бесплатные)

### 🥇 Вариант 1: Vercel (Frontend) + Railway (Backend)

**Почему:**
- ✅ Vercel - лучший хостинг для React, бесплатный
- ✅ Railway - простой деплой Node.js, бесплатный tier
- ✅ HTTPS из коробки, без промежуточных экранов
- ✅ Автоматический деплой из GitHub

**Стоимость:** Бесплатно для старта

---

### 🥈 Вариант 2: Vercel (Frontend + Backend)

**Почему:**
- ✅ Всё в одном месте
- ✅ Бесплатный tier достаточен для MVP
- ✅ Serverless функции для API
- ✅ Очень просто настроить

**Стоимость:** Бесплатно

---

### 🥉 Вариант 3: Render

**Почему:**
- ✅ Бесплатный хостинг для frontend и backend
- ✅ Простая настройка
- ✅ Автоматический HTTPS
- ✅ PostgreSQL включен в бесплатный tier

**Стоимость:** Бесплатно (с ограничениями)

---

## 📋 Подробные инструкции

### Вариант 1: Vercel + Railway (Рекомендуется)

#### Frontend на Vercel

1. **Установите Vercel CLI:**
   ```bash
   npm i -g vercel
   ```

2. **Войдите:**
   ```bash
   vercel login
   ```

3. **Деплой frontend:**
   ```bash
   cd frontend
   vercel
   ```
   
   Vercel автоматически:
   - Определит что это React приложение
   - Настроит build
   - Деплоит на HTTPS домен

4. **Настройте переменные окружения:**
   - После деплоя зайдите на vercel.com
   - Settings → Environment Variables
   - Добавьте: `VITE_API_URL=https://your-backend.railway.app/api`

5. **Передеплойте:**
   ```bash
   vercel --prod
   ```

#### Backend на Railway

1. **Установите Railway CLI:**
   ```bash
   npm i -g @railway/cli
   ```

2. **Войдите:**
   ```bash
   railway login
   ```

3. **Создайте проект:**
   ```bash
   cd backend
   railway init
   ```

4. **Добавьте PostgreSQL (опционально):**
   ```bash
   railway add postgresql
   ```
   
   Railway создаст переменную `DATABASE_URL` автоматически

5. **Настройте переменные:**
   ```bash
   railway variables
   ```
   
   Добавьте:
   - `PORT=5001`
   - `FRONTEND_URL=https://your-app.vercel.app`

6. **Деплой:**
   ```bash
   railway up
   ```

7. **Получите URL:**
   ```bash
   railway domain
   ```
   
   Скопируйте URL (например: `https://your-app.railway.app`)

8. **Обновите frontend:**
   - В Vercel обновите `VITE_API_URL` на URL из Railway

---

### Вариант 2: Всё на Vercel

#### Настройка

1. **Создайте `vercel.json` в корне проекта:**
   ```json
   {
     "version": 2,
     "builds": [
       {
         "src": "frontend/package.json",
         "use": "@vercel/static-build",
         "config": {
           "distDir": "dist"
         }
       },
       {
         "src": "backend/src/index.ts",
         "use": "@vercel/node"
       }
     ],
     "routes": [
       {
         "src": "/api/(.*)",
         "dest": "backend/src/index.ts"
       },
       {
         "src": "/(.*)",
         "dest": "frontend/$1"
       }
     ]
   }
   ```

2. **Обновите `frontend/vite.config.ts`:**
   ```typescript
   export default defineConfig({
     plugins: [react()],
     build: {
       outDir: 'dist'
     }
   })
   ```

3. **Деплой:**
   ```bash
   vercel
   ```

**⚠️ Примечание:** Этот вариант сложнее настроить, но всё в одном месте.

---

### Вариант 3: Render

#### Frontend на Render

1. Зайдите на [render.com](https://render.com)
2. New → Static Site
3. Подключите GitHub репозиторий
4. Настройки:
   - **Build Command:** `cd frontend && npm install && npm run build`
   - **Publish Directory:** `frontend/dist`
5. Добавьте переменные окружения:
   - `VITE_API_URL=https://your-backend.onrender.com/api`

#### Backend на Render

1. New → Web Service
2. Подключите GitHub репозиторий
3. Настройки:
   - **Root Directory:** `backend`
   - **Build Command:** `npm install && npm run build`
   - **Start Command:** `npm start`
4. Добавьте переменные:
   - `PORT=5001`
   - `FRONTEND_URL=https://your-frontend.onrender.com`
   - `DATABASE_URL` (если используете PostgreSQL)

---

## 🔧 Обновление кода для продакшена

### 1. Обновите `backend/src/index.ts` для PostgreSQL:

```typescript
// Измените в prisma/schema.prisma:
// provider = "postgresql"
// url = env("DATABASE_URL")

// После деплоя выполните миграции:
// railway run npx prisma migrate deploy
// или
// vercel env pull && npx prisma migrate deploy
```

### 2. Создайте `.env.example` файлы:

**backend/.env.example:**
```env
PORT=5001
FRONTEND_URL=https://your-app.vercel.app
DATABASE_URL=postgresql://...
```

**frontend/.env.example:**
```env
VITE_API_URL=https://your-backend.railway.app/api
```

### 3. Обновите Bot для нового URL:

```typescript
// bot/.env
WEBAPP_URL=https://your-app.vercel.app
```

И обновите в BotFather:
- `/mybots` → ваш бот → Menu Button
- URL: `https://your-app.vercel.app`

---

## 🎯 Рекомендация: Vercel + Railway

**Почему этот вариант:**
1. ✅ Самый простой в настройке
2. ✅ Бесплатный tier достаточен для MVP
3. ✅ Отличная документация
4. ✅ Автоматический HTTPS
5. ✅ Нет промежуточных экранов
6. ✅ Быстрый деплой

**Пошагово:**
1. Деплойте frontend на Vercel (2 минуты)
2. Деплойте backend на Railway (5 минут)
3. Обновите переменные окружения
4. Обновите WebApp URL в BotFather
5. Готово!

---

## 📝 Чеклист перед деплоем

- [ ] Код в GitHub репозитории
- [ ] `.env` файлы не закоммичены (в `.gitignore`)
- [ ] Переменные окружения подготовлены
- [ ] PostgreSQL или SQLite (SQLite только для тестов)
- [ ] Prisma schema настроен для PostgreSQL
- [ ] Frontend build работает локально
- [ ] Backend запускается локально

---

## 🚨 Частые проблемы

### CORS ошибки
- Убедитесь, что `FRONTEND_URL` в backend содержит правильный домен
- В production добавьте все возможные домены

### База данных
- Для продакшена используйте PostgreSQL (Railway, Render предоставляют бесплатно)
- SQLite не подходит для продакшена (не масштабируется)

### Переменные окружения
- Не используйте `.env` файлы в продакшене
- Настройте переменные в панели хостинга

---

## 💡 Следующие шаги

1. Выберите вариант (рекомендую Vercel + Railway)
2. Следуйте инструкциям выше
3. Обновите Bot URL в BotFather
4. Протестируйте в Telegram!

Нужна помощь с конкретным шагом? Скажите, какой вариант выбираете! 🚀
