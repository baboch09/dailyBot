# Добавление поля telegramName в базу данных

## ✅ Что было сделано

### 1. Обновлена схема Prisma
Добавлено поле `telegramName` в модель `User`:
```prisma
telegramName String?  // Username пользователя в Telegram (например, @username без @)
```

### 2. Создана миграция
Файл: `backend/prisma/migrations/20260128193344_add_telegram_name/migration.sql`

SQL команда:
```sql
ALTER TABLE "User" ADD COLUMN "telegramName" TEXT;
```

### 3. Обновлен Backend код
- `backend/src/middleware/auth.ts` - теперь получает и сохраняет username из заголовка `x-telegram-username`
- При создании пользователя сохраняется `telegramName`
- При последующих запросах username обновляется, если изменился

### 4. Обновлен Frontend код
- `frontend/src/utils/telegram.ts` - добавлена функция `getTelegramUsername()`
- `frontend/src/services/api.ts` - обновлен интерцептор для отправки username в заголовке

### 5. Сгенерирован Prisma Client
Prisma Client обновлен с новой схемой

---

## 🚀 Применение миграции к базе данных

### Вариант 1: Через Prisma CLI (рекомендуется)

```bash
cd backend

# Применить миграцию к базе данных
npx prisma migrate deploy
```

### Вариант 2: Вручную через SQL

Если у вас есть прямой доступ к PostgreSQL:

```bash
psql "your_database_url"
```

Затем выполните:
```sql
ALTER TABLE "User" ADD COLUMN "telegramName" TEXT;
```

### Вариант 3: Через Prisma Studio

```bash
cd backend
npx prisma studio
```

Откроется веб-интерфейс, но Prisma Studio не применяет миграции автоматически. 
Используйте Вариант 1 или 2.

---

## 🔍 Проверка миграции

### Проверить статус миграций:

```bash
cd backend
npx prisma migrate status
```

Должно показать, что миграция `20260128193344_add_telegram_name` применена.

### Через Prisma Studio:

```bash
cd backend
npx prisma studio
```

Откройте таблицу `User` и убедитесь, что появился столбец `telegramName`.

### Через SQL запрос:

```sql
-- Проверить структуру таблицы
\d "User"

-- Или
SELECT column_name, data_type, is_nullable 
FROM information_schema.columns 
WHERE table_name = 'User';
```

---

## 📝 Как это работает

### Backend
1. Когда пользователь делает запрос, frontend отправляет заголовок `x-telegram-username`
2. Middleware `authenticateUser` получает username из заголовка
3. При создании нового пользователя сохраняется `telegramName`
4. При последующих запросах, если username изменился, он обновляется в базе

### Frontend
1. Функция `getTelegramUsername()` получает username из Telegram WebApp API
2. API интерцептор автоматически добавляет заголовок `x-telegram-username` ко всем запросам
3. Username берется из `window.Telegram.WebApp.initDataUnsafe.user.username`

---

## ⚠️ Важные замечания

### Username может быть null
Не у всех пользователей Telegram есть username. Поле `telegramName` опциональное (`String?`).

### Username может меняться
Пользователи могут изменить свой username в настройках Telegram. Код автоматически обновляет его при каждом запросе.

### Формат username
Username хранится **без символа @**. Например, если пользователь `@john_doe`, то сохраняется `john_doe`.

---

## 🧪 Тестирование

### 1. Локальная разработка

```bash
# Запустите backend
cd backend
npm run dev

# Запустите frontend
cd frontend
npm run dev

# Откройте приложение через Telegram Mini App
```

### 2. Проверьте логи backend

При создании пользователя должно быть:
```
Created new user with telegram_id: 123456789 username: john_doe
```

При обновлении username:
```
Updated username for user: 123456789 new username: new_username
```

### 3. Проверьте базу данных

```bash
cd backend
npx prisma studio
```

Откройте таблицу `User` и проверьте, что `telegramName` заполняется корректно.

---

## 📊 Production деплой

### Vercel (автоматически)

Если вы используете Vercel с Vercel Postgres:

1. Сделайте commit и push изменений:
```bash
git add .
git commit -m "Add telegramName field to User model"
git push
```

2. Vercel автоматически:
   - Запустит `npx prisma generate`
   - Применит миграции через `npx prisma migrate deploy` (если настроено в build script)

3. Проверьте логи деплоя в Vercel Dashboard

### Другие платформы

Убедитесь, что в build script есть:
```json
{
  "scripts": {
    "build": "npx prisma generate && npx prisma migrate deploy && tsc"
  }
}
```

---

## 🎉 Готово!

После применения миграции поле `telegramName` будет автоматически заполняться для всех пользователей при их первом входе или следующем запросе к API.

Старые пользователи получат username при следующем визите в приложение.

---

**Дата создания:** 2026-01-28  
**Миграция:** `20260128193344_add_telegram_name`
