# Сводка изменений редизайна

## Что сделано

### 1. Редизайн главного экрана
- **Верхний блок:** иконка приложения, заголовок «Трекер привычек», подзаголовок «Каждый день — шаг к лучшей версии себя».
- **Счётчик привычек:** «Привычек: X из 3 / ∞» с визуальным акцентом (бейдж, отступы).
- **Статус подписки:**
  - **Free:** бейдж «Free», подпись «До 3 привычек», кнопка «Получить PRO».
  - **Pro:** цветная карточка (градиент), остаток дней, кнопка «Продлить».
- Улучшены отступы, типографика, адаптив (sm-брейкпоинты), сохранён стиль Telegram Mini App.

### 2. Экран подписки (Paywall)
- **Тарифы:** Месяц 79₽, Год 699₽ (по умолчанию, бейдж «Лучший выбор»), Навсегда 2199₽ в отдельном блоке.
- **Lifetime:** блок «Доступно до {дата}», визуально выделен (оранжевый градиент).
- **Преимущества PRO:** ∞ привычек, 🔔 Напоминания, 📊 Аналитика, 🎯 Цели, 🎨 Темы, 🤖 Умные советы (иконки/эмодзи).
- Бэкенд: добавлен план `lifetime` (цена 2199₽, durationDays 36525 для расчёта срока).

### 3. Интерфейс напоминаний
- **Проблема overflow решена:** вместо inline-блока в карточке привычки используется **bottom sheet**.
- **ReminderBottomSheet:** выезжает снизу, сегментированный выбор режима [День | Интервал | Неделя | Кастом]. Режим «День» полностью работает (TimePicker, превью «Напоминание: каждый день в HH:MM»). Интервал/Неделя/Кастом — заглушки «скоро» (под будущее расширение).
- В **HabitItem** и **AddHabitForm** напоминание настраивается через один и тот же sheet; без выхода за границы контейнера, с прокруткой при необходимости.

### 4. Система целей
- В **Add/Edit Habit** добавлена секция **«Цель»** (переключатель Вкл/Выкл, только для PRO).
- **Типы целей:** серия дней подряд (streak), количество раз в период (count), период (period). Пресеты: 21 / 30 / 90.
- **Превью прогресса:** в карточке привычки отображается, например, «21 день подряд — 7/21 выполнено» (для типа streak используется текущий streak с бэкенда).
- БД: миграция `20260205120000_add_habit_goals` — поля `goalEnabled`, `goalType`, `goalTarget`, `goalPeriodDays`. Обратная совместимость сохранена.

### 5. Ограничения Free / Pro
- **Free:** ≤3 привычки, напоминания и цели недоступны (при попытке включить — скролл к paywall и/или сообщение).
- **Pro:** без лимита привычек, все типы напоминаний (в UI), цели. При попытке использовать закрытую функцию показывается paywall (скролл к блоку подписки).

### 6. Совместимость с бэкендом и БД
- Текущие API не сломаны; добавлены только опциональные поля (цели, план lifetime).
- Миграция для целей добавлена; старый формат напоминаний (одно поле `reminderTime` HH:MM) сохранён.
- Валидация целей на сервере (express-validator); расписания напоминаний пока только «раз в день» (как раньше).

### 7. Качество кода
- Переиспользован общий компонент ReminderBottomSheet.
- Комментарии в контроллерах и миграции.
- Нейминг: ReminderMode, PlanTab, PRO_BENEFITS и т.д.

### 8. Тесты
- Добавлены тесты:
  - `backend/src/__tests__/subscription-limits.test.ts` — лимит привычек Free (3), логика canAdd.
  - `backend/src/__tests__/goal-progress.test.ts` — форматирование превью цели (streak).
- В `package.json` backend добавлены скрипты `test` и `test:watch` (vitest). Запуск: `npm install` затем `npm run test` в `backend/`.

---

## Что улучшено
- Визуальная иерархия и «премиальность» главного экрана и карточки подписки.
- Конверсия в PRO: явные кнопки «Получить PRO» / «Продлить», тариф «Лучший выбор», блок Lifetime с датой.
- Удержание: цели и превью прогресса (X/Y дней подряд).
- UX напоминаний: без overflow, один понятный bottom sheet с превью.
- Чёткое разграничение Free/Pro и показ paywall при попытке использовать закрытые функции.

---

## Что требует внимания в будущем
1. **Миграция БД:** перед деплоем выполнить `npx prisma migrate deploy` (или применить `20260205120000_add_habit_goals`) в целевой среде.
2. **Режимы напоминаний:** интервал / по дням недели / кастом пока только в UI (заглушки); для полной поддержки понадобится расширение схемы (например, JSON `reminderSchedule`) и доработка cron `api/reminders.ts`.
3. **Цели типа count/period:** прогресс «X раз за Y дней» пока не считается на бэкенде; при необходимости — отдельный endpoint или расширение ответа GET habits (например, `goalProgress`).
4. **Тесты:** после `npm install` в backend запустить `npm run test`; при добавлении новых сценариев — расширить тесты на планировщик и ограничения подписки.
5. **Корневая схема Prisma:** в `prisma/schema.prisma` (корень) добавлены поля целей; при использовании отдельной схемы для продакшена синхронизировать миграции.

---

Дата: 2025-02-05
