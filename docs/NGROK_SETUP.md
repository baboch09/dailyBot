# 🔐 Быстрая настройка Ngrok (2 минуты)

## Шаг 1: Регистрация (30 секунд)

1. Откройте: https://dashboard.ngrok.com/signup
2. Зарегистрируйтесь (можно через Google/GitHub)
3. После регистрации вы автоматически попадёте на страницу с authtoken

## Шаг 2: Установка токена (30 секунд)

Скопируйте ваш authtoken и выполните:

```bash
cd /Users/ebabochiev/Desktop/tg
./bin/ngrok config add-authtoken ВАШ_ТОКЕН_ЗДЕСЬ
```

Пример:
```bash
./bin/ngrok config add-authtoken 2abc123def456ghi789jkl012mno345pqr
```

## Шаг 3: Запуск туннеля (1 минута)

```bash
./setup-tunnel.sh
```

Или напрямую:
```bash
./bin/ngrok http 3000
```

## Шаг 4: Копирование URL

В выводе ngrok найдите строку типа:
```
Forwarding  https://abc123.ngrok-free.app -> http://localhost:3000
```

Скопируйте HTTPS URL (например: `https://abc123.ngrok-free.app`)

## Шаг 5: Настройка в BotFather

1. Откройте [@BotFather](https://t.me/botfather)
2. `/mybots` → выберите бота → **Bot Settings** → **Menu Button**
3. **Text**: `Открыть трекер`
4. **URL**: `https://abc123.ngrok-free.app` (ваш URL из шага 4)

## Готово! 🎉

Теперь в Telegram отправьте `/start` боту и нажмите "📱 Открыть трекер"

