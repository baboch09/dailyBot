# 🔧 Исправление ошибки Prisma "prepared statement already exists"

## ❌ Ошибка:
```
Error: prepared statement "s1" already exists
ConnectorError(ConnectorError { user_facing_error: None, kind: QueryError(PostgresError { code: "42P05", ... })
```

## 🔍 Причина:
Ошибка возникает при использовании **connection pooling** (Supabase pooler на порту 6543) с Prisma. Prisma пытается использовать prepared statements, которые не поддерживаются connection pooler.

---

## ✅ Решение:

### Вариант 1: Использовать прямой порт (Рекомендуется)

Измените `DATABASE_URL` в Vercel Environment Variables:

**Было (pooler порт 6543):**
```
postgresql://user:pass@host:6543/db?sslmode=require
```

**Стало (прямой порт 5432):**
```
postgresql://user:pass@host:5432/db?sslmode=require
```

### Вариант 2: Добавить параметр pgbouncer

Если используете pooler порт, добавьте параметр `?pgbouncer=true`:

```
postgresql://user:pass@host:6543/db?sslmode=require&pgbouncer=true
```

**Примечание:** Код уже обновлён для автоматического переключения на порт 5432 и добавления `pgbouncer=true`, если используется pooler.

---

## 🔄 Что было сделано в коде:

1. **Автоматическое переключение порта:** Если обнаружен порт 6543 (pooler), автоматически переключается на 5432 (прямой)
2. **Добавление параметра pgbouncer:** Автоматически добавляется `?pgbouncer=true` для pooler соединений
3. **Настройка Prisma Client:** Оптимизирована для serverless окружения

---

## 📋 Проверка в Vercel:

### 1. Проверьте DATABASE_URL в Environment Variables:

**Settings → Environment Variables → DATABASE_URL**

**Должен быть:**
```
postgresql://postgres.wftzanfybiwxuvpjpizh:W4S4YrBcTA5ZyxWS@aws-1-us-east-1.pooler.supabase.com:5432/postgres?sslmode=require
```

**Важно:** Используйте порт `5432`, а не `6543`

### 2. Передеплойте проект:

После изменения `DATABASE_URL`:
- Vercel Dashboard → Deployments → последний деплой → **Redeploy**

Или изменения применятся автоматически после push в GitHub.

---

## ✅ После исправления:

Ошибка "prepared statement already exists" должна исчезнуть, и приложение будет работать корректно.

---

## 💡 Важно:

- **Прямой порт (5432):** Поддерживает prepared statements, лучше для Prisma
- **Pooler порт (6543):** Не поддерживает prepared statements, требует специальных настроек
- **Для Supabase:** Рекомендуется использовать прямой порт для Prisma

---

**Проблема должна быть решена после обновления `DATABASE_URL` на порт 5432 в Vercel!** ✅
