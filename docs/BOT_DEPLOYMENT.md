# 🤖 Деплой Telegram бота

## 📋 Текущая ситуация

**Бот работает локально** на вашем компьютере и **не деплоится** на Vercel автоматически.

---

## 🏗️ Архитектура проекта

### 🌐 Vercel (автоматический деплой)
- ✅ **Frontend** (React) - `https://daily-bot-drab.vercel.app`
- ✅ **Backend API** (Express) - `/api/*`
- ✅ При изменениях в Git → **автоматический деплой**

### 🤖 Локальный бот (на вашем компьютере)
- ❌ **Telegram Bot** (`bot/src/index.ts`)
- ❌ Работает как отдельный процесс
- ❌ При изменениях в Git → **НЕ обновляется автоматически**

---

## 🔄 Когда нужно перезапускать бота?

### ❌ Всегда нужен перезапуск:
1. **При изменении `bot/.env`** (переменные окружения)
   - Переменные окружения читаются только при запуске
   - Пример: изменение `WEBAPP_URL`

### ✅ Автоматически перезапускается:
2. **При изменении `bot/src/index.ts`** (если запущен через `npm run dev`)
   - Используется `tsx watch` для автоматической перезагрузки
   - Перезапуск происходит автоматически при сохранении файла

### ⚠️ Требует перезапуска после Git изменений:
3. **После `git pull` или обновления кода из Git**
   - Если изменился `bot/.env` → перезапустить
   - Если изменился код бота → `tsx watch` перезапустит автоматически

---

## 💡 Решения для автоматической работы

### Вариант 1: Автоматическая перезагрузка кода (уже работает)

При запуске через `npm run dev`:
```bash
cd bot
npm run dev
```

Код автоматически перезагружается при изменении файлов (благодаря `tsx watch`).

**Но:** переменные окружения (`bot/.env`) всё равно требуют ручного перезапуска.

### Вариант 2: Деплой бота на хостинг (рекомендуется для production)

Разверните бота на хостинге, чтобы он работал постоянно и обновлялся автоматически:

#### 🚀 Варианты хостинга:

1. **Railway** (просто, дёшево)
   ```bash
   # Установите Railway CLI
   npm i -g @railway/cli
   
   # Деплой
   cd bot
   railway init
   railway up
   ```

2. **Render** (бесплатный план)
   - Создайте Web Service
   - Подключите GitHub репозиторий
   - Укажите: `cd bot && npm run dev`

3. **Heroku**
   ```bash
   heroku create your-bot-name
   cd bot
   git subtree push --prefix bot heroku main
   ```

4. **DigitalOcean App Platform**
   - Подключите GitHub
   - Настройте автодеплой из папки `bot`

#### 📋 Что нужно для деплоя бота:

1. **Environment Variables** на хостинге:
   ```
   TELEGRAM_BOT_TOKEN=ваш_токен
   WEBAPP_URL=https://daily-bot-drab.vercel.app
   ```

2. **Добавьте `Procfile` в `bot/`**:
   ```
   worker: npm run dev
   ```

3. **Настройте автодеплой** из папки `bot/`

---

## ✅ Преимущества деплоя бота:

- ✅ Работает 24/7 (не нужно держать компьютер включённым)
- ✅ Автоматическое обновление при изменениях в Git
- ✅ Не нужно перезапускать вручную
- ✅ Более надёжно для production

---

## 🔄 Текущая работа (локально):

### Для разработки:
```bash
cd bot
npm run dev
```

### После изменения `bot/.env`:
1. Остановите бота: `Ctrl+C`
2. Запустите заново: `npm run dev`

### После `git pull`:
1. Проверьте, изменился ли `bot/.env`
2. Если да → перезапустите бота
3. Если нет → `tsx watch` перезапустит автоматически

---

## 📝 Итог:

**Текущая ситуация:**
- ✅ Frontend/Backend на Vercel → автоматический деплой
- ❌ Бот локально → нужно перезапускать вручную при изменении `.env`

**Рекомендация:**
Для production разверните бота на хостинге (Railway, Render и т.д.) для автоматической работы.

---

**Нужна помощь с деплоем бота на хостинг?** 🚀
