# 🚀 Быстрый старт с Tunnel

## Все сервисы запущены! ✅

- ✅ Backend: http://localhost:5001
- ✅ Frontend: http://localhost:3000  
- ✅ Bot: Работает

## Шаг 1: Настройка туннеля (выберите один вариант)

### Вариант A: Ngrok (рекомендуется, требует регистрацию)

1. **Зарегистрируйтесь** (бесплатно): https://dashboard.ngrok.com/signup
2. **Получите authtoken**: https://dashboard.ngrok.com/get-started/your-authtoken
3. **Установите authtoken**:
   ```bash
   cd /Users/ebabochiev/Desktop/tg
   ./bin/ngrok config add-authtoken YOUR_TOKEN_HERE
   ```
4. **Запустите туннель**:
   ```bash
   ./setup-tunnel.sh
   ```
   Или напрямую:
   ```bash
   ./bin/ngrok http 3000
   ```

5. **Скопируйте HTTPS URL** (например: `https://abc123.ngrok-free.app`)

### Вариант B: Cloudflare Tunnel (без регистрации)

1. Установите:
   ```bash
   brew install cloudflare/cloudflare/cloudflared
   ```

2. Запустите:
   ```bash
   cloudflared tunnel --url http://localhost:3000
   ```

3. Скопируйте HTTPS URL из вывода

### Вариант C: Localtunnel (через npx, без установки)

```bash
npx localtunnel --port 3000
```

## Шаг 2: Настройка WebApp URL в BotFather

1. Откройте [@BotFather](https://t.me/botfather) в Telegram
2. Отправьте: `/mybots`
3. Выберите вашего бота
4. Выберите: **Bot Settings** → **Menu Button** → **Configure Menu Button**
5. Введите:
   - **Text**: `Открыть трекер`
   - **URL**: Ваш HTTPS URL из шага 1 (например: `https://abc123.ngrok-free.app`)

## Шаг 3: Тестирование

1. Откройте Telegram
2. Найдите вашего бота
3. Отправьте: `/start`
4. Нажмите кнопку **"📱 Открыть трекер"**
5. Приложение должно открыться! 🎉

## Автоматический скрипт

Запустите:
```bash
chmod +x setup-tunnel.sh
./setup-tunnel.sh
```

Скрипт проверит frontend и запустит доступный туннель.

## Важно

- **Для локальной разработки обязательно нужен HTTPS туннель** (Telegram требует HTTPS)
- Туннель должен быть запущен всё время работы приложения
- Если туннель перестанет работать, просто перезапустите его и обновите URL в BotFather
