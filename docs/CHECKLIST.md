# ✅ Чеклист: Готово ли приложение к работе?

## 📋 Что уже готово:

- ✅ **Код проекта** - полностью написан и исправлен
- ✅ **Backend API** - Express + Prisma + TypeScript
- ✅ **Frontend** - React + TypeScript + Vite
- ✅ **Telegram Bot** - node-telegram-bot-api
- ✅ **База данных** - Prisma schema для PostgreSQL
- ✅ **Деплой конфигурация** - vercel.json, vercel-build.sh
- ✅ **Все ошибки исправлены** - TypeScript, Prisma, Build

---

## 🔴 Что нужно сделать для работы:

### 1. ✅ Деплой на Vercel (сделано?)
   - [ ] Проект задеплоен на Vercel
   - [ ] Получен URL (например: `https://habits-tracker.vercel.app`)
   - [ ] Frontend доступен по URL
   - [ ] API endpoint `/api/health` работает

### 2. 🔴 Создать PostgreSQL базу данных
   - [ ] Vercel Dashboard → Storage → Create Database → Postgres
   - [ ] Название: `habits-tracker-db`
   - [ ] Vercel автоматически создаст `POSTGRES_URL`

### 3. 🔴 Настроить переменные окружения
   - [ ] Vercel Dashboard → Settings → Environment Variables
   - [ ] Добавить `FRONTEND_URL` = ваш Vercel URL
   - [ ] `POSTGRES_URL` появится автоматически после создания БД

### 4. 🔴 Применить миграции базы данных
   - [ ] После создания PostgreSQL
   - [ ] Передеплойте проект (миграции применятся автоматически)
   - [ ] Или вручную через Vercel CLI

### 5. 🔴 Обновить Telegram Bot
   - [ ] Обновить `bot/.env`: `WEBAPP_URL=https://your-project.vercel.app`
   - [ ] BotFather → `/mybots` → ваш бот → Menu Button
   - [ ] Установить URL: `https://your-project.vercel.app`

### 6. 🔴 Запустить Bot локально (или на сервере)
   - [ ] Запустить: `cd bot && npm run dev`
   - [ ] Bot должен работать и отвечать на `/start`

---

## 🧪 Как проверить, что всё работает:

### Шаг 1: Проверка Frontend
```
https://your-project.vercel.app
```
Должна открыться страница приложения.

### Шаг 2: Проверка API
```
https://your-project.vercel.app/api/health
```
Должен вернуть: `{"status":"ok",...}`

### Шаг 3: Проверка в Telegram
1. Откройте Telegram
2. Найдите вашего бота
3. Отправьте: `/start`
4. Должна появиться кнопка "📱 Открыть трекер"
5. Нажмите на кнопку
6. Приложение должно открыться в Telegram!

---

## ⚠️ Важно:

### Без PostgreSQL:
- ❌ Приложение НЕ будет работать (ошибки при запросах к API)
- ❌ Невозможно создать/сохранить привычки

### Без обновления Bot URL:
- ❌ Кнопка в Telegram не откроет приложение
- ❌ Mini App не запустится

### Без запущенного Bot:
- ❌ Бот не будет отвечать на `/start`
- ❌ Нельзя открыть Mini App

---

## ✅ Минимальные требования для работы:

1. ✅ Проект задеплоен на Vercel
2. ✅ PostgreSQL создана в Vercel
3. ✅ Переменные окружения настроены
4. ✅ Bot URL обновлён в BotFather
5. ✅ Bot запущен (локально или на сервере)

---

## 🚀 Быстрая проверка:

Выполните эти команды для проверки:

```bash
# 1. Проверить Frontend
curl https://your-project.vercel.app

# 2. Проверить API
curl https://your-project.vercel.app/api/health

# 3. Проверить Bot (должен быть запущен)
# Откройте Telegram и отправьте /start боту
```

---

## 📞 Если что-то не работает:

1. **Frontend не открывается:**
   - Проверьте URL в браузере
   - Проверьте логи в Vercel Dashboard

2. **API не работает:**
   - Проверьте, что PostgreSQL создана
   - Проверьте переменные окружения
   - Проверьте логи функций в Vercel

3. **Bot не отвечает:**
   - Убедитесь, что bot запущен: `cd bot && npm run dev`
   - Проверьте токен в `bot/.env`

4. **Mini App не открывается:**
   - Проверьте URL в BotFather
   - Убедитесь, что URL начинается с `https://`
   - Попробуйте открыть URL напрямую в браузере

---

**Ответ:** Приложение будет работать **только после выполнения всех пунктов выше** ✅
