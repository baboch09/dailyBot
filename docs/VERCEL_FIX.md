# 🔧 Исправление проблем с Vercel

## Ваш URL: https://habits-tracker-blush.vercel.app

## Что было исправлено:

1. ✅ **Упрощена интеграция Express с Vercel** - `api/index.ts` теперь напрямую экспортирует Express app
2. ✅ **Добавлен `@vercel/node`** в `backend/package.json` (нужен для serverless functions)
3. ✅ **Улучшен build процесс** - правильная установка всех зависимостей
4. ✅ **Исправлены routes** в `vercel.json`

---

## 📋 Что нужно сделать сейчас:

### 1. Запушьте изменения в Git:

```bash
git push origin main
```

### 2. Vercel автоматически передеплоит проект

После пуша, Vercel начнёт новый деплой (если подключён GitHub).

Или вручную:
- Vercel Dashboard → Deployments → Trigger Deployment

### 3. Проверьте Environment Variables в Vercel:

Vercel Dashboard → Settings → Environment Variables:

✅ **Должны быть:**
- `FRONTEND_URL` = `https://habits-tracker-blush.vercel.app`
- `POSTGRES_URL` = (автоматически после создания PostgreSQL)
- `DATABASE_URL` = (можно не указывать, будет использован POSTGRES_URL)

**⚠️ Важно:** После изменения переменных окружения - **передеплойте проект!**

---

## 🧪 Проверка после деплоя:

### 1. Health Check:
```bash
curl https://habits-tracker-blush.vercel.app/api/health
```

**Ожидаемый результат:**
```json
{"status":"ok","timestamp":"...","environment":"production"}
```

### 2. API Endpoint (нужен telegram_id):
```bash
curl https://habits-tracker-blush.vercel.app/api/habits \
  -H "x-telegram-id: 123456789"
```

**Ожидаемый результат:**
```json
[]
```
(пустой массив, если нет привычек)

### 3. Frontend:
Откройте: https://habits-tracker-blush.vercel.app

**Проверьте в DevTools (F12):**
- Console - нет ошибок
- Network - запросы к `/api/habits` должны работать

---

## 🐛 Если всё ещё не работает:

### Проблема: `/api/health` возвращает 404

**Решение:**
1. Проверьте логи: Vercel Dashboard → Functions → Logs
2. Убедитесь, что файлы `api/health.ts` и `api/index.ts` закоммичены
3. Проверьте, что `vercel.json` правильно настроен

### Проблема: Frontend показывает ошибку

**Решение:**
1. Откройте DevTools (F12) → Console
2. Посмотрите, какая ошибка
3. Проверьте Network tab - какие запросы падают

### Проблема: "Cannot find module '@vercel/node'"

**Решение:**
1. Проверьте, что `@vercel/node` добавлен в `backend/package.json`
2. Передеплойте проект

---

## 📝 Структура проекта для Vercel:

```
/
├── api/
│   ├── health.ts        ← Health check endpoint
│   └── index.ts         ← Main API (Express app)
├── backend/
│   └── src/
│       └── index.ts     ← Express приложение (экспортируется)
├── frontend/
│   └── dist/            ← Собранный frontend (outputDirectory)
├── vercel.json          ← Конфигурация Vercel
└── vercel-build.sh      ← Build скрипт
```

---

## 🚀 После успешного деплоя:

1. ✅ Проверьте `/api/health` - должен работать
2. ✅ Откройте frontend в Telegram
3. ✅ Обновите URL бота в BotFather:
   ```
   https://habits-tracker-blush.vercel.app
   ```

---

## 💡 Полезные команды:

```bash
# Локальный тест build
./vercel-build.sh

# Проверка API локально (если нужно)
cd backend && npm run dev
```
