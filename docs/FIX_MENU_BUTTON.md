# 🔧 Исправление Menu Button

## ❌ Проблема:
Menu Button перестал работать после изменений.

## 🔍 Причина:
Menu Button настраивается **только в BotFather** и не зависит от `bot/.env`. Если он сломался, возможно:
1. URL в BotFather устарел или неправильный
2. Само приложение на Vercel недоступно или выдаёт ошибки
3. Проблема с аутентификацией в приложении

---

## ✅ Решение:

### Шаг 1: Проверьте Menu Button в BotFather

1. Откройте Telegram → [@BotFather](https://t.me/botfather)
2. Отправьте: `/mybots`
3. Выберите вашего бота
4. Выберите: **Bot Settings → Menu Button**
5. Проверьте URL:
   - Должен быть: `https://daily-bot-drab.vercel.app`
   - Если другой URL — обновите на правильный
6. Нажмите **Save**

### Шаг 2: Проверьте доступность приложения

**В браузере откройте:**
```
https://daily-bot-drab.vercel.app
```

**Должно:**
- Открыться интерфейс приложения
- Может быть ошибка аутентификации (это нормально, если открыто напрямую)
- Но интерфейс должен загрузиться

**Проверьте API:**
```
https://daily-bot-drab.vercel.app/api/health
```

**Должен вернуть:**
```json
{
  "status": "ok",
  "timestamp": "...",
  "environment": "production"
}
```

### Шаг 3: Проверьте консоль браузера

**При открытии через Menu Button:**

1. Откройте приложение через Menu Button в боте
2. Нажмите три точки (⋮) → **Developer Tools** или **Открыть в браузере**
3. Или откройте на компьютере и нажмите **F12**
4. Проверьте консоль на ошибки:
   - Ошибки загрузки ресурсов
   - Ошибки JavaScript
   - Сообщения о `telegram_id`

**Что искать:**
```
✅ Got telegram_id from initDataUnsafe: 123456789
✅ Added x-telegram-id header: 123456789
```

**Или ошибки:**
```
❌ Telegram WebApp not available
❌ No telegram_id available
❌ Error loading habits
```

### Шаг 4: Убедитесь, что приложение открывается через Telegram

**Важно:**
- Menu Button должен открывать приложение **внутри Telegram**
- Не открывайте URL напрямую в браузере для тестирования
- Только через Menu Button в боте

---

## 🔄 Если ничего не помогает:

### Вариант 1: Пересоздать Menu Button

1. BotFather → `/mybots` → ваш бот
2. **Bot Settings → Menu Button → Remove Menu Button**
3. Затем создайте заново:
   - **Bot Settings → Menu Button → Configure Menu Button**
   - **Text**: `Открыть трекер`
   - **URL**: `https://daily-bot-drab.vercel.app`
   - **Save**

### Вариант 2: Использовать /newapp

1. BotFather → `/newapp`
2. Выберите вашего бота
3. Заполните:
   - **Title**: `Habits Tracker`
   - **Description**: `Track your daily habits`
   - **Web App URL**: `https://daily-bot-drab.vercel.app`
4. Это создаст новое Web App и обновит Menu Button

---

## 📋 Чеклист проверки:

- [ ] URL в BotFather правильный: `https://daily-bot-drab.vercel.app`
- [ ] Приложение открывается в браузере (даже с ошибкой аутентификации)
- [ ] API `/api/health` возвращает `{"status":"ok"}`
- [ ] Приложение открывается через Menu Button **внутри Telegram**
- [ ] В консоли браузера нет критических ошибок
- [ ] `telegram_id` извлекается (проверьте консоль)

---

## 💡 Важно:

**Menu Button и кнопка `/start` — это разные вещи:**
- **Menu Button** — настраивается в BotFather, всегда отображается в боте
- **Кнопка `/start`** — создаётся кодом бота, использует `bot/.env`

Оба должны вести на один и тот же URL: `https://daily-bot-drab.vercel.app`

---

После исправления обе кнопки должны работать! ✅
