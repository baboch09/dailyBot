# Настройка Telegram Mini App

## Текущий статус ✅

- ✅ **Backend API**: Работает на `http://localhost:5001`
- ✅ **Frontend**: Работает на `http://localhost:3000`
- ✅ **Telegram Bot**: Запущен и работает
- ✅ **База данных**: Настроена (SQLite)

## Шаг 1: Настройка WebApp URL в BotFather

Для того, чтобы приложение открывалось в Telegram, нужно настроить WebApp URL.

### Вариант A: Локальная разработка (через ngrok)

1. Установите ngrok:
   ```bash
   brew install ngrok
   # или скачайте с https://ngrok.com/download
   ```

2. Запустите туннель для frontend:
   ```bash
   ngrok http 3000
   ```

3. Скопируйте HTTPS URL (например: `https://abc123.ngrok.io`)

4. Откройте [@BotFather](https://t.me/botfather) в Telegram

5. Отправьте команду:
   ```
   /mybots
   ```

6. Выберите вашего бота

7. Выберите "Bot Settings" → "Menu Button" → "Configure Menu Button"

8. Введите:
   - **Text**: Открыть трекер (или любое другое название)
   - **URL**: `https://abc123.ngrok.io` (ваш ngrok URL)

### Вариант B: Production (с хостингом)

1. Задеплойте frontend на Vercel, Netlify или другой хостинг
2. Получите production URL (например: `https://your-app.vercel.app`)
3. Обновите WebApp URL в BotFather (как в варианте A)
4. Обновите `WEBAPP_URL` в `bot/.env` на production URL

## Шаг 2: Тестирование

1. Откройте Telegram
2. Найдите вашего бота
3. Отправьте команду `/start`
4. Нажмите кнопку "📱 Открыть трекер"
5. Приложение должно открыться в Telegram!

## Проверка работы

### Проверка бота:
```bash
# Отправьте /start боту в Telegram
# Должна появиться кнопка "Открыть трекер"
```

### Проверка API:
```bash
# Тест health endpoint
curl http://localhost:5001/health

# Тест создания привычки (нужен telegram_id из Telegram)
curl -X POST http://localhost:5001/api/habits \
  -H "Content-Type: application/json" \
  -H "x-telegram-id: 123456789" \
  -d '{"name": "Тестовая привычка"}'
```

## Возможные проблемы

### Бот не отвечает
- Проверьте, что бот запущен: `ps aux | grep bot`
- Проверьте токен в `bot/.env`
- Проверьте логи: `tail -f /tmp/bot.log`

### WebApp не открывается
- Убедитесь, что frontend запущен на порту 3000
- Проверьте URL в BotFather
- Для локальной разработки обязательно используйте ngrok (Telegram требует HTTPS)
- Проверьте, что в `bot/.env` правильный `WEBAPP_URL`

### API не работает
- Проверьте, что backend запущен: `curl http://localhost:5001/health`
- Проверьте, что frontend правильно настроен: `cat frontend/src/services/api.ts | grep API_URL`
- Убедитесь, что telegram_id правильно передаётся (проверьте в Network tab браузера)

## Перезапуск всех сервисов

```bash
# Остановить все
pkill -f "tsx watch"
pkill -f "vite"

# Запустить backend
cd backend && npm run dev &

# Запустить frontend  
cd frontend && npm run dev &

# Запустить bot
cd bot && npm run dev &
```

## Полезные команды

```bash
# Логи backend
tail -f /tmp/backend.log

# Логи bot
tail -f /tmp/bot.log

# Проверка портов
lsof -i :3000 -i :5001
```
