# 🔧 Исправление ошибки "unverified commit" в Vercel

## Проблема

Vercel отменяет деплои с ошибкой:
```
The Deployment was canceled because it was created with an unverified commit
```

## Причина

GitHub требует, чтобы все коммиты были сделаны с **подтверждённым email адресом**, связанным с вашим аккаунтом GitHub.

---

## ✅ Решение

### Шаг 1: Проверьте email в GitHub

1. Откройте настройки GitHub: https://github.com/settings/emails
2. Убедитесь, что ваш email **подтверждён** (зелёная галочка ✅)
3. Если email не подтверждён:
   - Нажмите "Verify" или "Resend verification email"
   - Проверьте почту и подтвердите email

### Шаг 2: Настройте Git с правильным email

```bash
# Узнайте ваш подтверждённый email из GitHub
# Затем установите его в Git:

git config user.email "your-verified-email@example.com"
git config user.name "Your Name"
```

**Важно:** Используйте **точно тот же email**, который подтверждён в GitHub!

### Шаг 3: Пересоздайте коммит (рекомендуется)

```bash
# Пересоздайте последний коммит с правильным автором
git commit --amend --reset-author --no-edit

# Запушьте изменения (--force-with-lease безопаснее чем --force)
git push --force-with-lease origin main
```

### Альтернатива: Создайте новый коммит

Если не хотите переписывать историю:

```bash
# Создайте новый коммит с правильным автором
git commit --allow-empty -m "Fix: Update deployment verification"

# Запушьте
git push origin main
```

---

## 🔍 Проверка

### Проверить текущие настройки Git:

```bash
git config user.email
git config user.name
```

### Проверить автора последних коммитов:

```bash
git log --format="%h %an <%ae>" -5
```

Убедитесь, что email совпадает с подтверждённым email в GitHub.

---

## ⚠️ Важно

1. **Email должен быть подтверждён** в GitHub (Settings → Emails)
2. **Git config должен использовать тот же email**, что и в GitHub
3. После изменения настроек **нужно создать новый коммит** или пересоздать последний

---

## 🚀 После исправления

1. Vercel автоматически начнёт новый деплой
2. Коммит будет проверен GitHub
3. Деплой должен пройти успешно

---

## 📝 Дополнительно

### Если проблема остаётся:

1. Проверьте, что email подтверждён в GitHub
2. Убедитесь, что используете правильный email в Git config
3. Создайте новый коммит после исправления настроек
4. Проверьте настройки Vercel → Settings → Git → Connected Git Repository

### Настройка глобально (для всех проектов):

```bash
git config --global user.email "your-verified-email@example.com"
git config --global user.name "Your Name"
```

---

## ✅ Быстрое решение

```bash
# 1. Установите правильный email (замените на ваш подтверждённый email из GitHub)
git config user.email "your-verified-email@example.com"

# 2. Пересоздайте последний коммит
git commit --amend --reset-author --no-edit

# 3. Запушьте изменения
git push --force-with-lease origin main
```

После этого Vercel автоматически начнёт новый деплой с проверенным коммитом! 🎉
