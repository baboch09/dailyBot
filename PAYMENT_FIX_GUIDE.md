# 🚨 Инструкция: Платеж прошел, но подписка не активировалась

## 📋 Проблема

Деньги списались с карты, но:
- ❌ Подписка осталась: `free` / `expired`
- ❌ Платеж в базе: `status = "pending"`
- ✅ Деньги списались в реальности

**Причина:** Webhook от YooKassa не дошел или не обработался корректно.

---

## 🔧 Решение 1: Автоматический скрипт (Рекомендуется)

### Запустить скрипт проверки всех pending платежей:

```bash
cd /Users/ebabochiev/Desktop/tg
npx tsx scripts/fix-pending-payment.ts
```

Скрипт автоматически:
1. ✅ Найдет все pending платежи
2. ✅ Проверит их статус в YooKassa
3. ✅ Обновит статус в базе данных
4. ✅ Активирует подписку, если платеж succeeded

### Проверить конкретный платеж:

```bash
npx tsx scripts/fix-pending-payment.ts <payment_id>
```

---

## 🔧 Решение 2: Через Prisma Studio (Визуальный интерфейс)

### Шаг 1: Откройте базу данных

```bash
cd backend
npx prisma studio
```

### Шаг 2: Найдите платеж

1. Откройте таблицу `Payment`
2. Найдите платеж со статусом `pending`
3. Скопируйте `yookassaId`

### Шаг 3: Проверьте в YooKassa

Зайдите в личный кабинет YooKassa:
- https://yookassa.ru/my/payments

Найдите платеж по ID и посмотрите реальный статус.

### Шаг 4: Обновите вручную

Если статус в YooKassa = `succeeded`:

**В Prisma Studio обновите платеж:**
1. Таблица `Payment` → найдите платеж
2. Измените `status` на `succeeded`
3. Сохраните

**Затем обновите пользователя:**
1. Таблица `User` → найдите по `userId` из платежа
2. Обновите:
   - `subscriptionType` = `premium`
   - `subscriptionStatus` = `active`
   - `subscriptionExpiresAt` = текущая дата + 30 дней (для месяца) или + 365 (для года)
   - `subscriptionStartedAt` = текущая дата (если не было)
3. Сохраните

---

## 🔧 Решение 3: Через API endpoint

Если backend запущен, используйте API для проверки:

```bash
# Проверить последний платеж пользователя
curl -X GET http://localhost:5001/api/subscription/check-latest-payment \
  -H "x-telegram-id: <TELEGRAM_ID>"
```

Это автоматически:
- Проверит статус в YooKassa
- Обновит базу
- Активирует подписку

---

## 🔍 Диагностика проблемы

### 1. Проверьте настройку Webhook в YooKassa

Зайдите: https://yookassa.ru/my/merchant/integration/http-notifications

**Должно быть:**
- ✅ URL: `https://your-app.vercel.app/api/payments/webhook`
- ✅ События: `payment.succeeded`, `payment.canceled`
- ✅ Статус: Активен

**Проверьте логи webhook:**
- В личном кабинете YooKassa есть раздел "HTTP-уведомления"
- Там видны все попытки отправки webhook
- Если там ошибки - webhook не доходит до сервера

### 2. Проверьте логи Vercel

```bash
# Если используете Vercel CLI
vercel logs
```

Ищите:
- `📦 YooKassa webhook received`
- `✅ Payment xxx processed successfully`
- `✅ Subscription activated`

Если этих логов нет - webhook не приходит.

### 3. Типичные причины

❌ **Webhook URL неправильный**
   - Должен быть: `https://your-domain.com/api/payments/webhook`
   - НЕ должно быть: `/api/subscription/webhook` или другой путь

❌ **Webhook не настроен вообще**
   - Забыли настроить в личном кабинете YooKassa

❌ **Serverless функция "заснула"**
   - На Vercel Free Plan функции могут "засыпать"
   - Первый запрос может быть медленным

❌ **Ошибка проверки подписи**
   - В production режиме проверяется подпись webhook
   - Если подпись неверная - webhook игнорируется
   - Смотрите логи: `❌ Invalid webhook signature`

❌ **Ошибка в обработке**
   - Баг в коде обработки webhook
   - Смотрите логи: `❌ Error processing webhook`

---

## 📊 SQL запросы для проверки

### Найти все pending платежи:

```sql
SELECT 
  p.id,
  p."yookassaId",
  p.status,
  p.amount,
  p."createdAt",
  u."telegramId",
  u."subscriptionStatus"
FROM "Payment" p
JOIN "User" u ON u.id = p."userId"
WHERE p.status = 'pending'
ORDER BY p."createdAt" DESC;
```

### Обновить статус платежа вручную:

```sql
-- ВНИМАНИЕ: Используйте только если точно знаете, что платеж succeeded в YooKassa!
UPDATE "Payment"
SET status = 'succeeded', "updatedAt" = NOW()
WHERE id = 'PAYMENT_ID';
```

### Активировать подписку вручную:

```sql
-- Месячная подписка (30 дней)
UPDATE "User"
SET 
  "subscriptionType" = 'premium',
  "subscriptionStatus" = 'active',
  "subscriptionStartedAt" = NOW(),
  "subscriptionExpiresAt" = NOW() + INTERVAL '30 days',
  "updatedAt" = NOW()
WHERE id = 'USER_ID';

-- Годовая подписка (365 дней)
UPDATE "User"
SET 
  "subscriptionType" = 'premium',
  "subscriptionStatus" = 'active',
  "subscriptionStartedAt" = NOW(),
  "subscriptionExpiresAt" = NOW() + INTERVAL '365 days',
  "updatedAt" = NOW()
WHERE id = 'USER_ID';
```

---

## 🛠️ Как предотвратить в будущем

### 1. Настройте мониторинг

Добавьте алерты на:
- Платежи, висящие в `pending` больше 10 минут
- Webhook ошибки в логах

### 2. Добавьте fallback проверку

В коде уже есть проверка при возврате пользователя:
- `GET /api/subscription/check-latest-payment`
- Автоматически вызывается, когда пользователь возвращается в приложение после оплаты

### 3. Проверьте webhook в тестовом режиме

Перед production:
1. Сделайте тестовый платеж
2. Проверьте, что webhook пришел
3. Проверьте логи обработки

### 4. Используйте polling как fallback (только для development)

В файле `backend/src/controllers/subscription.controller.ts` есть код:

```typescript
if (config.nodeEnv !== 'production' || process.env.ENABLE_PAYMENT_POLLING === 'true') {
  // Проверка статуса через 5 секунд
}
```

Но это НЕ работает в serverless (Vercel), так как функция завершается сразу.

---

## 📞 Экстренные контакты

### Если ничего не помогает:

1. **YooKassa поддержка:**
   - 📧 support@yookassa.ru
   - 📱 8 (800) 250-66-99
   - Скажите: "Платеж прошел, деньги списались, но webhook не пришел"
   - Они могут переотправить webhook вручную

2. **Возврат денег:**
   - Если не можете активировать подписку
   - Сделайте возврат через личный кабинет YooKassa
   - Клиент получит деньги обратно

---

## ✅ Чек-лист после исправления

- [ ] Статус платежа изменен на `succeeded`
- [ ] Подписка пользователя `subscriptionStatus = 'active'`
- [ ] Дата окончания `subscriptionExpiresAt` установлена правильно
- [ ] Пользователь видит активную подписку в приложении
- [ ] Webhook настроен правильно в YooKassa
- [ ] Проверены логи - webhook приходит и обрабатывается

---

## 📝 Шаблон для записи инцидента

```
Дата: YYYY-MM-DD HH:MM
Проблема: Платеж не активировал подписку
Payment ID: xxx
YooKassa ID: xxx
User Telegram ID: xxx
Сумма: XXX RUB
Время платежа: YYYY-MM-DD HH:MM
Способ решения: [скрипт / вручную / API]
Причина: [webhook не пришел / ошибка в коде / другое]
Решено: [да / нет]
Комментарий: ...
```

---

**Дата создания:** 2026-01-28  
**Последнее обновление:** 2026-01-28
