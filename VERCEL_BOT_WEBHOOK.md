# 🤖 Настройка Telegram бота через Webhooks на Vercel

## ✅ Преимущества:

- ✅ Бот работает на Vercel вместе с frontend/backend
- ✅ Не нужно держать локальный процесс запущенным
- ✅ Автоматическое обновление при изменениях в Git
- ✅ Работает 24/7 без вашего компьютера

---

## 📋 Пошаговая настройка:

### Шаг 1: Добавить Environment Variables в Vercel

**Vercel Dashboard → Settings → Environment Variables:**

1. Добавьте `TELEGRAM_BOT_TOKEN`:
   ```
   Name: TELEGRAM_BOT_TOKEN
   Value: ваш_токен_бота
   Environments: Production, Preview, Development
   ```

2. Добавьте `WEBAPP_URL`:
   ```
   Name: WEBAPP_URL
   Value: https://daily-bot-drab.vercel.app
   Environments: Production, Preview, Development
   ```

3. Нажмите **Save**

### Шаг 2: Задеплоить на Vercel

После добавления переменных окружения Vercel автоматически задеплоит проект.

Или вручную:
```bash
vercel --prod
```

### Шаг 3: Настроить Webhook в Telegram

**После деплоя получите URL вашего проекта:**
- Обычно: `https://daily-bot-drab.vercel.app`
- Или ваш кастомный домен

**Настройте webhook через Telegram API:**

```bash
curl -X POST "https://api.telegram.org/bot<YOUR_BOT_TOKEN>/setWebhook" \
  -d "url=https://daily-bot-drab.vercel.app/api/bot"
```

**Или через браузер:**
```
https://api.telegram.org/bot<YOUR_BOT_TOKEN>/setWebhook?url=https://daily-bot-drab.vercel.app/api/bot
```

Замените `<YOUR_BOT_TOKEN>` на ваш токен бота.

### Шаг 4: Проверить Webhook

```bash
curl "https://api.telegram.org/bot<YOUR_BOT_TOKEN>/getWebhookInfo"
```

Должен вернуть:
```json
{
  "ok": true,
  "result": {
    "url": "https://daily-bot-drab.vercel.app/api/bot",
    "has_custom_certificate": false,
    "pending_update_count": 0
  }
}
```

### Шаг 5: Протестировать

1. Откройте вашего бота в Telegram
2. Отправьте команду `/start`
3. Должен ответить бот, запущенный на Vercel!

---

## 🔄 Переключение с Polling на Webhooks

**Важно:** После настройки webhook:

1. **Остановите локальный бот** (если запущен):
   ```bash
   # Найдите процесс и остановите
   pkill -f "bot.*tsx"
   ```

2. **Бот теперь работает на Vercel** и не нужен локальный процесс

3. **Для локальной разработки** можете оставить локальный бот, но для production используйте webhook на Vercel

---

## 🐛 Устранение неполадок

### Бот не отвечает:

1. Проверьте, что webhook настроен:
   ```bash
   curl "https://api.telegram.org/bot<TOKEN>/getWebhookInfo"
   ```

2. Проверьте логи в Vercel:
   - Dashboard → Deployments → выберите деплой → Function Logs

3. Проверьте, что переменные окружения установлены:
   - `TELEGRAM_BOT_TOKEN`
   - `WEBAPP_URL`

### Ошибка 405 Method Not Allowed:

- Убедитесь, что используете POST запрос
- Проверьте URL: должен быть `/api/bot`

### Удалить webhook (вернуться к polling):

```bash
curl -X POST "https://api.telegram.org/bot<TOKEN>/deleteWebhook"
```

---

## ✅ Итог:

После настройки бот будет работать на Vercel автоматически:
- ✅ Не нужен локальный процесс
- ✅ Обновляется при изменениях в Git
- ✅ Работает 24/7

---

**Готово! Теперь весь проект (frontend + backend + bot) работает на Vercel!** 🎉
