# 🔗 Настройка Webhook в YooKassa

## 📋 Быстрая инструкция

### Шаг 1: Зайдите в личный кабинет YooKassa
https://yookassa.ru/my

### Шаг 2: Перейдите в настройки уведомлений
```
Настройки → Уведомления → HTTP-уведомления
```

### Шаг 3: Добавьте webhook

**URL webhook:**
```
https://daily-bot-drab.vercel.app/api/payments/webhook
```

**События для подписки:**
- ✅ `payment.succeeded` - Платеж успешно завершен
- ✅ `payment.canceled` - Платеж отменен

**Дополнительные параметры:**
- Метод: `POST`
- Формат: `JSON`

### Шаг 4: Сохраните настройки

Нажмите **"Сохранить"**

---

## ✅ Проверка webhook

### 1. YooKassa отправит тестовый webhook

После сохранения YooKassa автоматически отправит тестовое уведомление.

### 2. Проверьте логи Vercel

```bash
vercel logs --follow
```

**Должны увидеть:**
```
📦 YooKassa webhook received: payment.succeeded xxx
✅ Webhook signature validated successfully
🔄 Updating payment status: xxx
```

### 3. Если webhook не работает

**Проверьте:**
1. URL правильный: `https://daily-bot-drab.vercel.app/api/payments/webhook`
2. События выбраны: `payment.succeeded`, `payment.canceled`
3. Метод: `POST`
4. Формат: `JSON`

**В логах ищите:**
```
❌ Error processing webhook
❌ Invalid webhook signature
❌ Webhook signature is missing
```

---

## 🧪 Тестирование webhook вручную

Можно протестировать webhook вручную (только для отладки):

```bash
curl -X POST https://daily-bot-drab.vercel.app/api/payments/webhook \
  -H "Content-Type: application/json" \
  -H "x-yookassa-signature: test_signature" \
  -d '{
    "type": "payment.succeeded",
    "object": {
      "id": "test_payment_id",
      "status": "succeeded"
    }
  }'
```

**Ответ должен быть:**
```json
{"received":true}
```

---

## 📱 Webhook для разработки (локально)

Если нужно тестировать webhook локально, используйте ngrok:

```bash
# Запустите ngrok
ngrok http 5001

# Используйте ngrok URL для webhook
# Пример: https://xxxx-xx-xx-xx-xx.ngrok-free.app/api/payments/webhook
```

**Внимание:** Каждый раз при перезапуске ngrok URL меняется!

---

## 🔒 Безопасность

### В production режиме:

✅ Webhook подпись **проверяется обязательно**
✅ Используется SHA-256 хеширование
✅ Защита от поддельных уведомлений

### В test режиме:

🧪 Webhook подпись **пропускается**
🧪 Для удобства тестирования

---

## 📊 Что делает webhook

1. **Получает уведомление** от YooKassa о статусе платежа
2. **Проверяет подпись** (в production)
3. **Обновляет статус** платежа в БД
4. **Активирует подписку** если платеж успешен
5. **Логирует результат**

### Пример логов успешного webhook:

```
📦 YooKassa webhook received: payment.succeeded 12345
   type: payment.succeeded
   paymentId: 12345
   status: succeeded
   mode: production
✅ Webhook signature validated successfully
🔄 Updating payment status: 12345 pending -> succeeded
🆕 Activating new subscription
   Plan: month
   Duration: 30 days
✅ Subscription activated for user abc123
   Expires at: 2026-02-28T10:00:00.000Z
✅ Payment 12345 processed successfully
```

---

## 🆘 Troubleshooting

### Проблема: "Webhook signature is missing"

**Причина:** YooKassa не отправляет подпись

**Решение:**
1. Проверьте настройки webhook в личном кабинете
2. Убедитесь что используете боевой ключ (live_)
3. Обратитесь в поддержку YooKassa

### Проблема: "Invalid webhook signature"

**Причина:** Неправильная проверка подписи

**Решение:**
1. Проверьте что `YUKASSA_SECRET_KEY` правильный
2. Проверьте что `YUKASSA_MODE=production`
3. Посмотрите логи: ожидаемая vs полученная подпись

### Проблема: Webhook не приходит

**Причина:** URL недоступен или неправильный

**Решение:**
1. Проверьте что приложение задеплоено на Vercel
2. Проверьте URL: `https://daily-bot-drab.vercel.app/api/payments/webhook`
3. Проверьте что endpoint отвечает:
   ```bash
   curl https://daily-bot-drab.vercel.app/api/health
   ```

---

## 📞 Поддержка

Если webhook не работает:
1. Проверьте логи Vercel
2. Проверьте настройки в YooKassa
3. Обратитесь в поддержку: support@yookassa.ru

---

**Дата:** 2026-01-28  
**Статус:** ✅ Готово к настройке
