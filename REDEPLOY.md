# 🔄 Как передеплоить на Vercel

## Способ 1: Через Vercel Dashboard (самый простой)

1. **Зайдите на [vercel.com](https://vercel.com)**
   - Войдите в аккаунт

2. **Откройте ваш проект**
   - Найдите проект в списке (например, `habits-tracker`)

3. **Передеплойте:**
   - Откройте вкладку **Deployments** (вверху)
   - Найдите последний deployment
   - Нажмите на три точки (⋮) справа от deployment
   - Выберите **Redeploy**
   - Выберите **Use existing Build Cache** (или без него, если хотите чистой сборки)
   - **Redeploy**

4. **Готово!** Деплой начнётся автоматически

---

## Способ 2: Через GitHub (автоматически)

Если проект подключён к GitHub:

1. **Запушьте изменения:**
   ```bash
   git push
   ```

2. **Vercel автоматически задеплоит** новую версию!

Вы увидите статус в Vercel Dashboard → Deployments

---

## Способ 3: Через Vercel CLI

```bash
cd /Users/ebabochiev/Desktop/tg

# Передеплой production
npx vercel --prod

# Или просто деплой (preview)
npx vercel
```

---

## Способ 4: Создать новый коммит (для триггера деплоя)

Если нужно передеплоить без изменений:

```bash
cd /Users/ebabochiev/Desktop/tg
git commit --allow-empty -m "Trigger redeploy"
git push
```

Vercel автоматически задеплоит новую версию.

---

## 📋 Проверка деплоя

После передеплоя:

1. **Откройте ваш URL:** `https://your-project.vercel.app`
2. **Проверьте API:** `https://your-project.vercel.app/api/health`
3. **Проверьте логи:** Vercel Dashboard → Deployments → выберите deployment → Logs

---

## 🔧 Если деплой не работает

### Проверьте логи:
- Vercel Dashboard → Deployments → последний deployment → Logs
- Найдите ошибки в логах

### Частые проблемы:

**Build ошибка:**
- Проверьте, что все файлы закоммичены
- Проверьте переменные окружения
- Убедитесь, что скрипты исполняемые

**Runtime ошибка:**
- Проверьте переменные окружения (Settings → Environment Variables)
- Проверьте, что PostgreSQL создан (если используете)
- Проверьте логи функций: Functions → Logs

---

**Самый простой способ:** Vercel Dashboard → Deployments → Redeploy 🚀
