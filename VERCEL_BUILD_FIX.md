# 🔧 Исправление ошибки деплоя на Vercel

## Проблема

Ошибка при деплое связана с тем, что:
1. Миграции Prisma пытаются выполниться без базы данных
2. Конфигурация `vercel.json` использует старый формат `builds`

## Решение

### 1. Обновлён `vercel.json`

Я обновил конфигурацию:
- ✅ Убрал `builds` (старый формат)
- ✅ Использую `buildCommand` напрямую
- ✅ Миграции НЕ выполняются при build (они нужны только после создания БД)

### 2. Порядок действий при деплое

**ВАЖНО:** Сначала создайте PostgreSQL, ПОТОМ примените миграции!

#### Шаг 1: Первый деплой (без миграций)

1. **Передеплойте проект:**
   ```bash
   npx vercel --prod
   ```
   Или через веб-интерфейс Vercel - просто нажмите Redeploy

2. **Создайте PostgreSQL:**
   - Vercel Dashboard → ваш проект
   - **Storage** → **Create Database** → **Postgres**
   - Название: `habits-tracker-db`
   - **Create**

#### Шаг 2: Настройка переменных окружения

1. **Settings** → **Environment Variables**
2. Добавьте:
   ```
   DATABASE_PROVIDER = postgresql
   FRONTEND_URL = https://your-project.vercel.app
   ```
   (`POSTGRES_URL` уже будет автоматически после создания БД)

#### Шаг 3: Применение миграций

Есть несколько способов:

##### Вариант A: Через Vercel CLI (после настройки переменных)

```bash
# Получить переменные
npx vercel env pull .env.local

# Применить миграции
cd backend
export DATABASE_URL=$(grep POSTGRES_URL ../.env.local | cut -d '=' -f2)
npx prisma migrate deploy
npx prisma generate
```

##### Вариант B: Добавить миграции в Build Command (после создания БД)

После создания PostgreSQL, обновите `vercel.json`:

```json
{
  "buildCommand": "cd backend && npm install && npx prisma generate && npx prisma migrate deploy && npm run build && cd ../frontend && npm install && npm run build"
}
```

Затем передеплойте.

##### Вариант C: Через Vercel Dashboard (рекомендуется)

1. Vercel Dashboard → ваш проект
2. **Settings** → **Functions**
3. Добавьте в Environment Variables:
   - `POSTGRES_URL` (уже есть после создания БД)
   - `DATABASE_PROVIDER` = `postgresql`

4. Создайте новую функцию для миграций (или используйте существующую):
   - **Deployments** → выберите последний deployment
   - **Functions** → откройте `api/index.ts`
   - Или создайте временную функцию для миграций

**Проще всего:** после создания БД просто передеплойте с обновлённым `vercel.json` который включает миграции.

#### Шаг 4: Обновлённый buildCommand (после создания БД)

После того как PostgreSQL создан и `POSTGRES_URL` доступен, обновите `vercel.json`:

```json
{
  "buildCommand": "cd backend && npm install && npx prisma generate && npx prisma migrate deploy && npm run build && cd ../frontend && npm install && npm run build"
}
```

Или временно отключите миграции, если они падают:

```json
{
  "buildCommand": "cd backend && npm install && npx prisma generate && npm run build && cd ../frontend && npm install && npm run build"
}
```

Миграции можно применить вручную после первого деплоя.

---

## Быстрое решение

1. **Передеплойте** с текущей конфигурацией (без миграций)
2. **Создайте PostgreSQL** в Vercel Dashboard
3. **Добавьте переменные окружения**
4. **Примените миграции вручную:**
   ```bash
   npx vercel env pull
   cd backend
   export POSTGRES_URL="ваш_url_из_vercel"
   npx prisma migrate deploy
   ```
5. **Готово!**

---

## Проверка работы

После деплоя проверьте:

1. **Frontend:** `https://your-project.vercel.app`
2. **API Health:** `https://your-project.vercel.app/api/health`
3. **Логи:** Vercel Dashboard → Deployments → Logs

Если API возвращает ошибку - проверьте логи в Functions.

---

## Частые ошибки

### "Prisma schema not found"
- Убедитесь, что команды выполняются из правильной директории
- Проверьте пути в `buildCommand`

### "Migration failed"
- Убедитесь, что PostgreSQL создан
- Проверьте `POSTGRES_URL` в переменных окружения
- Попробуйте применить миграции вручную

### "Cannot find module"
- Проверьте, что `npm install` выполняется в обеих папках
- Убедитесь, что все зависимости в `package.json`
