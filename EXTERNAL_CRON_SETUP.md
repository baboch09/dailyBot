# 🔄 Настройка внешнего Cron для напоминаний

## ❌ Проблема с Vercel Cron

На бесплатном плане Vercel (Hobby) есть ограничения:
- Максимум **2 cron-джоба** на аккаунт
- Каждый может срабатывать **не чаще одного раза в день**

Наша задача проверяет напоминания каждую минуту, что превышает лимиты.

## ✅ Решение: Внешний Cron сервис

Используем бесплатный внешний сервис для вызова нашего endpoint.

---

## 🚀 Вариант 1: cron-job.org (Рекомендуется)

### Шаг 1: Зарегистрируйтесь на cron-job.org

1. Перейдите на https://cron-job.org
2. Создайте бесплатный аккаунт
3. Подтвердите email

### Шаг 2: Создайте Cron Job

1. Войдите в панель управления
2. Нажмите **"Create cronjob"**

3. Заполните форму:
   - **Title**: `Habits Reminders Check`
   - **Address (URL)**: 
     ```
     https://daily-bot-drab.vercel.app/api/reminders
     ```
     (Замените на ваш URL проекта)
   
   - **Schedule**: Выберите частоту
     - **Рекомендуется**: `Every 5 minutes` или `Every 15 minutes`
     - Для более точных напоминаний: `Every 5 minutes`
   
   - **Request Method**: `POST`
   
   - **Request Headers**: Добавьте заголовок для авторизации:
     ```
     Authorization: Bearer YOUR_CRON_SECRET
     ```
     (См. шаг 3 для получения секрета)

4. Нажмите **"Create cronjob"**

### Шаг 3: Установите CRON_SECRET в Vercel

1. Сгенерируйте случайный секретный ключ:
   ```bash
   openssl rand -hex 32
   ```
   Или используйте любой длинный случайный строку

2. Добавьте в Vercel Environment Variables:
   - **Vercel Dashboard** → **Settings** → **Environment Variables**
   - **Name**: `CRON_SECRET`
   - **Value**: ваш сгенерированный секрет
   - **Environments**: Production, Preview, Development
   - Нажмите **Save**

3. Используйте этот же секрет в заголовке `Authorization` в cron-job.org

### Шаг 4: Проверьте работу

1. В cron-job.org нажмите **"Run now"** для тестирования
2. Проверьте логи в Vercel:
   - **Dashboard** → **Deployments** → выберите деплой → **Function Logs**
3. Должен вернуться ответ: `{"success": true, ...}`

---

## 🚀 Вариант 2: EasyCron

1. Зарегистрируйтесь на https://www.easycron.com
2. Создайте новый cron job:
   - **URL**: `https://daily-bot-drab.vercel.app/api/reminders`
   - **HTTP Method**: `POST`
   - **HTTP Headers**: `Authorization: Bearer YOUR_CRON_SECRET`
   - **Schedule**: `*/5 * * * *` (каждые 5 минут)
3. Сохраните и активируйте

---

## 🚀 Вариант 3: GitHub Actions (Бесплатно)

Создайте файл `.github/workflows/reminders.yml`:

```yaml
name: Check Reminders

on:
  schedule:
    - cron: '*/5 * * * *'  # Каждые 5 минут
  workflow_dispatch:  # Для ручного запуска

jobs:
  check-reminders:
    runs-on: ubuntu-latest
    steps:
      - name: Call Reminders API
        run: |
          curl -X POST https://daily-bot-drab.vercel.app/api/reminders \
            -H "Authorization: Bearer ${{ secrets.CRON_SECRET }}"
```

1. Создайте секрет в GitHub:
   - **Settings** → **Secrets and variables** → **Actions**
   - **New repository secret**
   - **Name**: `CRON_SECRET`
   - **Value**: ваш секрет (тот же, что в Vercel)

2. Файл автоматически запустится по расписанию

---

## 🔒 Безопасность

**Важно:** Всегда используйте `CRON_SECRET` для защиты endpoint от несанкционированного доступа!

Без секрета любой может вызвать ваш endpoint и отправить напоминания.

---

## ⚙️ Настройка частоты

### Рекомендуемые интервалы:

- **Каждые 5 минут** (`*/5 * * * *`) - для точных напоминаний
- **Каждые 15 минут** (`*/15 * * * *`) - баланс между точностью и нагрузкой
- **Каждые 30 минут** (`*/30 * * * *`) - минимальная нагрузка

**Примечание:** Напоминания будут отправлены в течение выбранного интервала после наступления времени. Например, если напоминание в 09:00, а проверка каждые 5 минут, оно будет отправлено между 09:00 и 09:05.

---

## 🧪 Тестирование

### Ручной вызов для тестирования:

```bash
curl -X POST https://daily-bot-drab.vercel.app/api/reminders \
  -H "Authorization: Bearer YOUR_CRON_SECRET"
```

Должен вернуть:
```json
{
  "success": true,
  "timestamp": "2024-01-01T12:00:00.000Z",
  "processed": 5,
  "sent": 2
}
```

---

## ✅ Итог

После настройки внешнего cron:
- ✅ Деплой на Vercel не будет блокироваться
- ✅ Напоминания будут работать автоматически
- ✅ Можно настроить любую частоту проверки
- ✅ Бесплатно (на всех предложенных сервисах)

---

## 📝 Чек-лист

- [ ] Удалён cron из `vercel.json` (уже сделано)
- [ ] Сгенерирован `CRON_SECRET`
- [ ] Добавлен `CRON_SECRET` в Vercel Environment Variables
- [ ] Настроен внешний cron сервис (cron-job.org / EasyCron / GitHub Actions)
- [ ] Добавлен заголовок `Authorization: Bearer <CRON_SECRET>` в cron job
- [ ] Протестирован endpoint вручную
- [ ] Проверена работа автоматических запусков

---

**Готово! Теперь напоминания работают через внешний cron, и деплой на Vercel не блокируется!** 🎉
