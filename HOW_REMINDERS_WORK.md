# ⏰ Как работает система напоминаний

## 📋 Полный процесс работы

### 1️⃣ Установка напоминания

**Пользователь:**
1. Создаёт новую привычку или редактирует существующую
2. Устанавливает время напоминания (например, `09:00`)
3. Включает переключатель напоминания
4. Сохраняет изменения

**Что происходит:**
- Данные сохраняются в базу данных PostgreSQL
- В таблице `Habit` сохраняются поля:
  - `reminderTime`: `"09:00"` (формат HH:MM)
  - `reminderEnabled`: `true`

---

### 2️⃣ Автоматическая проверка (Cron Job)

**Внешний сервис (cron-job.org):**
- Вызывает endpoint каждые **5 минут** (настраивается)
- URL: `https://daily-bot-drab.vercel.app/api/reminders`
- Method: `POST`
- Headers: `Authorization: Bearer YOUR_CRON_SECRET`

**Vercel Serverless Function:**
- Получает запрос от cron сервиса
- Проверяет авторизацию (CRON_SECRET)
- Запускает функцию `checkAndSendReminders()`

---

### 3️⃣ Проверка привычек

**Что проверяется:**

```typescript
// 1. Получаем текущее время
const now = new Date()
const currentTime = "09:00" // формат HH:MM

// 2. Получаем все привычки с включёнными напоминаниями
const habits = await prisma.habit.findMany({
  where: {
    reminderEnabled: true,
    reminderTime: { not: null }
  }
})

// 3. Для каждой привычки проверяем:
for (const habit of habits) {
  // Наступило ли время напоминания?
  if (habit.reminderTime === currentTime) {
    // Выполнена ли привычка сегодня?
    const todayLog = await prisma.habitLog.findFirst({
      where: {
        habitId: habit.id,
        date: { /* сегодняшняя дата */ }
      }
    })
    
    // Если НЕ выполнена → отправляем напоминание
    if (!todayLog) {
      sendReminder(chatId, habitName)
    }
  }
}
```

**Проверки:**
- ✅ Время напоминания совпадает с текущим временем?
- ✅ Привычка НЕ выполнена сегодня?
- ✅ Напоминание включено?

---

### 4️⃣ Отправка напоминания

**Если все условия выполнены:**

1. **Получаем chatId пользователя:**
   - Из `habit.user.telegramId`
   - Преобразуем в число

2. **Отправляем сообщение в Telegram:**
   ```typescript
   bot.sendMessage(chatId, `
     ⏰ Напоминание!
     
     Не забудьте выполнить привычку: **${habitName}**
     
     Сегодня ещё есть время! 💪
   `, {
     parse_mode: 'Markdown',
     reply_markup: {
       inline_keyboard: [[
         { text: '📱 Открыть трекер', web_app: { url: webAppUrl } }
       ]]
     }
   })
   ```

3. **Пользователь получает:**
   - 📱 Сообщение в Telegram
   - ⏰ Текст напоминания
   - 🔘 Кнопку "Открыть трекер"

---

### 5️⃣ Примеры работы

#### Пример 1: Утренняя зарядка
- **Время напоминания:** `07:00`
- **Текущее время:** `07:03` (cron проверил через 3 минуты)
- **Статус привычки:** НЕ выполнена сегодня
- **Результат:** ✅ Напоминание отправлено в 07:03

#### Пример 2: Привычка уже выполнена
- **Время напоминания:** `09:00`
- **Текущее время:** `09:05`
- **Статус привычки:** ✅ Уже выполнена сегодня (в 08:30)
- **Результат:** ❌ Напоминание НЕ отправлено

#### Пример 3: Напоминание отключено
- **Время напоминания:** `10:00`
- **Текущее время:** `10:05`
- **reminderEnabled:** `false`
- **Результат:** ❌ Привычка не проверяется

---

## ⚙️ Технические детали

### Частота проверки

**Рекомендуется:** каждые **5 минут**

**Почему 5 минут:**
- ✅ Достаточно точно для напоминаний
- ✅ Не слишком частая нагрузка на сервер
- ✅ Напоминание придёт в течение 5 минут после установленного времени

**Альтернативы:**
- Каждые **15 минут** - экономия ресурсов
- Каждые **1 минуту** - максимальная точность (но больше нагрузка)

### Точность времени

**Текущая реализация:**
- Проверка происходит каждые 5 минут
- Если напоминание в `09:00`, оно будет отправлено между `09:00` и `09:05`
- Точность: **±5 минут**

**Примеры:**
- Напоминание в `09:00` → отправка в `09:00`, `09:05`, `09:10` (при следующей проверке)
- Напоминание в `09:03` → отправка в `09:05` (ближайшая проверка)

### Часовой пояс

**Текущая реализация:**
- Используется **UTC** (всемирное координированное время)
- Время в базе данных хранится в формате `HH:MM`

**Важно:**
- Если вы устанавливаете `09:00` для напоминания в 9 утра по местному времени
- Убедитесь, что учитываете разницу с UTC

**Будущее улучшение:**
- Сохранение часового пояса пользователя
- Автоматический расчёт времени с учётом часового пояса

---

## 🔧 Настройка

### Установка времени напоминания

**Через интерфейс:**
1. Создайте/отредактируйте привычку
2. Включите переключатель "Напоминание"
3. Выберите время (например, `09:00`)
4. Сохраните

**Через API:**
```bash
PUT /api/habits/:id
{
  "reminderTime": "09:00",
  "reminderEnabled": true
}
```

### Отключение напоминания

**Через интерфейс:**
- Выключите переключатель "Напоминание"
- Или удалите время напоминания

**Через API:**
```bash
PUT /api/habits/:id
{
  "reminderEnabled": false
}
```

---

## 📊 Логи и мониторинг

### Что логируется

```javascript
// Успешная отправка
console.log(`✅ Sent reminder for habit "${habit.name}" to user ${chatId}`)

// Статистика проверки
console.log(`📊 Processed ${habits.length} habits, sent ${sentCount} reminders`)

// Ошибки
console.error('Error sending reminder:', error)
```

### Проверка работы

**Ручной вызов:**
```bash
curl -X POST https://daily-bot-drab.vercel.app/api/reminders \
  -H "Authorization: Bearer YOUR_CRON_SECRET"
```

**Ответ:**
```json
{
  "success": true,
  "timestamp": "2024-01-21T09:05:00.000Z",
  "processed": 5,
  "sent": 2
}
```

**Где посмотреть логи:**
- **Vercel Dashboard** → **Deployments** → выберите деплой → **Function Logs**
- **cron-job.org** → History → смотреть статус выполнения

---

## ✅ Итог

**Система напоминаний работает автоматически:**

1. ✅ Пользователь устанавливает время напоминания
2. ✅ Внешний cron вызывает endpoint каждые 5 минут
3. ✅ Система проверяет все привычки
4. ✅ Если время совпадает и привычка не выполнена → отправка в Telegram
5. ✅ Пользователь получает напоминание с кнопкой "Открыть трекер"

**Преимущества:**
- ✅ Автоматическая работа 24/7
- ✅ Не беспокоит, если привычка уже выполнена
- ✅ Простая настройка через интерфейс
- ✅ Работает через бесплатные сервисы

---

**Готово! Система напоминаний полностью функциональна!** 🎉
